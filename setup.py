from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project uses data from Zillow aggregated for single-family residences at the county level for the state of Ohio to predict median home prices by county in the state of Ohio.',
    author='Markson O. Ofuoku',
    license='MIT',
)
